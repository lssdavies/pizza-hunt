//Import the models into this file, it will package up all the models
const Pizza = require("./Pizza");
const Comment = require("./Comment");

module.exports = { Pizza, Comment };
//this will be imported in ./controllers/pizza-controller.js
